spring:
    profiles:
        active:
            - local
    data:
        mongodb:
            username: root
            password: root
            database: strategy-test
logging:
    file:
        name: log/application-test.log
